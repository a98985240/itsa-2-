/*[C_AR03-易] 計算陣列中所有元素的立方和*/
#include <iostream>
#include <algorithm>
#include <string.h>
#include <cmath>
using namespace std;

int main()
{
    int array[6];
    int total=0;
    for(int i=0;i<6;i++)
    {
        cin >> array[i];
        total += (array[i]*array[i]*array[i]);
    }
    cout << total << endl ;

}
