/*[C_MM16-易] 判斷座標是否在圓形的範圍內*/
#include <iostream>
#include <algorithm>
#include <string.h>
#include <cmath>
using namespace std;

int main()
{
    int x,y;
    while(scanf("%d %d",&x,&y)!=EOF)
    {
        if(sqrt(x*x+y*y)<=100)
            cout <<"inside" <<endl;
        else
            cout <<"outside"<<endl;
    }
}
